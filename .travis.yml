dist: trusty
language: cpp
compiler: gcc

addons:
  apt:
    packages:
    - cmake
    - libboost-dev
    - protobuf-compiler
    - libprotobuf-dev
    - libnl-3-dev
    - libnl-genl-3-dev
    - libnl-route-3-dev
    - asn1c
    
script:
- cmake .
- make
- make package
